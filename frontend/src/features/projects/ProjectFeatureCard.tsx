import { Link } from "react-router-dom";
import type { ProjectListItem } from "./types";

export function ProjectFeatureCard({ project }: { project: ProjectListItem }) {
  return <article className="feature-project-card"><div className="feature-project-icon">{project.name.slice(0, 2).toUpperCase()}</div><span className={`role-badge ${project.currentUserRole.toLowerCase()}`}>{project.currentUserRole}</span><span className={`status ${project.status.toLowerCase()}`}>{project.status}{project.isReadOnly ? " · Read-only" : ""}</span><h2>{project.name}</h2><p>{project.description || "No description has been added yet."}</p><div className="feature-project-meta"><span>{project.defaultLanguage}</span><span>{project.memberCount} member{project.memberCount === 1 ? "" : "s"}</span>{project.deadlineAt && <span>Due {new Date(project.deadlineAt).toLocaleDateString()}</span>}</div><Link to={`/projects/${project.id}/workspace`}>Open workspace <span>→</span></Link><Link to={`/projects/${project.id}/pull-requests`}>Pull requests <span>→</span></Link><Link to={`/projects/${project.id}/board`}>Kanban board <span>→</span></Link><Link to={`/projects/${project.id}/settings`}>Project settings <span>→</span></Link></article>;
}
