using System;
namespace Coding.Enums
{
    public enum NotificationType
    {
        Invitation,
        Message,
        AICompleted,
        ProjectUpdated,
        Warning,
        TaskAssignment,
        UserMention,
        DirectMessage,
        RoleChange,
        TaskDeadlineExceeded
    }
}
